#Compare two values: if even, return smallest; if odd, return biggest.

def compare(x, y):
    if True:
        x % 2 == 0 and y % 2 == 0
        print(min(x, y))
    elif False:
        print(max(x, y))
    else:
        print("Enter integers only.")

compare(2, 6)
compare(7, 9)
compare(7, 7)
compare(7, 7.5)
